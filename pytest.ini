[pytest]
testpaths = tests
# importlib mode lets same-named test modules coexist in different directories
# without __init__.py files.
addopts = --import-mode=importlib
markers =
    slow: reads a real dataset off disk rather than a fixture; deselect with -m "not slow"
