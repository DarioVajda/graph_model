{
  // Smoke sweep: tiny knobs, one 2-value axis. Used to dry-run the sbatch path
  // (writes sbatch_commands.sh + job scripts, submits nothing) and, with
  // "mode": "local", to do a fast end-to-end check once a .gtds cache exists.
  "name": "smoke",
  "results_dir": "src/experiments/kgqa/results",

  "execution": {
    "mode": "sbatch",
    "sbatch": {
      "granularity": "per_config",
      "max_concurrent": 2,            // throttled job array (--array=0-(K-1)%2)
      "partition": "frida",
      "account": "povejmo",
      "gpus": "B200:1",
      "cpus": 8,
      "mem": "64G",
      "time": "01:00:00",             // smoke: minutes of work, not the 12h train budget
      "container": "/shared/workspace/povejmo/containers/transformers_deepspeed_latest.sqsh",
      "dry_run": false
    }
  },

  "mode": "train",
  "k_hop": [0, 2],                    // the only axis -> 2 runs

  // Enough optimizer steps (32 = 256 examples) to hit all common flex (L, N)
  // buckets and reach post-compile steady-state step timing; num_workers matches
  // the real runs so the timing extrapolates.
  "lora_r": 8,
  "seed": 0,
  "num_epochs": 1,
  "max_steps": 32,
  "eval_steps": 16,
  "gen_max_samples": 8,
  "num_workers": 4,
  "wandb_project": null
}
