## Backward Compatibility with RoPE

In this experiment, We test whether or not the outputs will be the same for the GraphLlamaModel and the default LlamaModel on the same sigle node graph.

✅ Assumption found to be true

### Run the experiment with the following command:

```
python3 -m src.experiments.backward_compatibility
```